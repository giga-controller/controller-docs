import { Callout } from "nextra-theme-docs";

# OAuth 2.0 Authentication

Check out the [common instructions](./common) for setting up all Google Suite applications.

# Using Google Docs

The following types of commands are supported (for now). Drop us a message if you need something else!

## Create a document

You can create a Google Doc by specifying the relevant fields.

```
Create a document titled User Feedback and add the following content -> Ship faster and break things if necessary
```
